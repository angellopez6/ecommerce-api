const { resolve } = require("path");

module.exports = {
  config: resolve("build/db/config.js"),
  "seeders-path": resolve("build/db/seeders"),
  "migrations-path": resolve("build/db/migrations"),
  "models-path": resolve("build/db/models"),
};
